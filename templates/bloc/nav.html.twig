{% block nav %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbar">
            <a class="navbar-brand" href="/">{{ 'project.title'|trans }}</a>
            <ul class="navbar-nav ml-auto mt-2 mt-lg-0">
                <li class="nav-item mx-2">
                    <a href="{{ url('register') }}" class="btn btn-info">{{ 'register.here'|trans }}</a>
                </li>
                <li class="nav-item mx-2">
                    <a href="{{ url('security_login') }}" class="btn btn-success">{{ 'login.login'|trans }}</a>
                </li>
            </ul>
        </div>
    </nav>
{% endblock %}
