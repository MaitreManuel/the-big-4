{% extends 'base.html.twig' %}

{% block title %}{{ 'page.register.form.meta.title'|trans|raw }}{% endblock %}

{% block body %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-4">
            <h1 class="text-center my-5">{{ 'form.register.title'|trans|raw }}</h1>
            {{ form(form) }}
        </div>
    </div>
</div>
{% endblock %}
